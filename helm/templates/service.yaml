apiVersion: v1
kind: Service
metadata:
  name: {{ include "tarrasqueapp.fullname" . }}
  labels:
    {{- include "tarrasqueapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tarrasqueapp.selectorLabels" . | nindent 4 }}
