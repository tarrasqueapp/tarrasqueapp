# TODO: Re-enable this workflow when the sandbox environment is created
# name: '[SANDBOX] Deploy'

# on:
#   workflow_dispatch:
#   pull_request:

# jobs:
#   deploy:
#     uses: ./.github/workflows/deploy-base.yaml

#     with:
#       ENVIRONMENT: sandbox
#       ENVIRONMENT_URL: https://sandbox.tarrasque.app
#       KUBE_NAMESPACE: tarrasqueapp-sandbox
#       HELM_ARGS: |
#         --set ingress.annotations."cert-manager\.io/cluster-issuer"="letsencrypt-staging" \
#         --set ingress.hosts[0].host="sandbox.tarrasque.app",ingress.hosts[0].paths[0].path="/()(.*)",ingress.hosts[0].paths[0].pathType="Prefix" \
#         --set ingress.tls[0].secretName="tls-secret-tarrasqueapp",ingress.tls[0].hosts[0]="sandbox.tarrasque.app" \

#     secrets: inherit
