version: '3.7'
services:
  #################
  ### Front-end ###
  #################

  client:
    build:
      context: ./apps/client
      dockerfile: Dockerfile.prod

  ################
  ### Back-end ###
  ################

  server:
    build:
      context: ./apps/server
      dockerfile: Dockerfile.prod

  ######################
  ### Infrastructure ###
  ######################

  caddy:
    volumes:
      - ./apps/caddy/Caddyfile.prod:/etc/caddy/Caddyfile:ro
