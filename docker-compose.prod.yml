version: '3.7'
services:
  #################
  ### Front-end ###
  #################

  client:
    build:
      context: ./packages/client
      dockerfile: Dockerfile.prod

  ################
  ### Back-end ###
  ################

  server:
    build:
      context: ./packages/server
      dockerfile: Dockerfile.prod

  ######################
  ### Infrastructure ###
  ######################

  caddy:
    volumes:
      - ./packages/caddy/Caddyfile.prod:/etc/caddy/Caddyfile:ro
